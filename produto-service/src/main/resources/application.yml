spring:
  application:
    name: produto-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/produtos_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:produto}
    password: ${DB_PASSWORD:produto123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
